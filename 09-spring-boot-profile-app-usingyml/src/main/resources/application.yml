spring:
  datasource:
    username: root   
    password: nikhilhayatpur@123
    url: jdbc:mysql:///spring
---
spring:
  config:
    activate:
      on-profile:
      - dev
  datasource:
    type: org.apache.commons.dbcp2.BasicDataSource   
---
spring:
  config:
    activate:
      on-profile:
      - qa
  datasource:
    type: org.apache.tomcat.jdbc.pool.DataSource
---
spring:
  config:
    activate:
      on-profile:
      - uat
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
---
spring:
  config:
    activate:
      on-profile:
      - prod
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
---
spring:
  config:
    activate:
      on-profile:
      - prodst
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
